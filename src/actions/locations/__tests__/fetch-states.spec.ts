describe.todo('fetchStates(country) unit tests', () => {
	beforeEach(() => {
		vi.resetModules();
	});

	// TODO: implementar testes de fetchStates

	it('recovers sorted static states when country is Brazil', async () => {});

	it('fetches and sort Brazil states from API when static data is empty or cant be resolved', async () => {});

	it('fetches non-Brazil states correctly ordered and formatted from API ', async () => {});

	it('throws a readable error when states fetching goes wrong', async () => {});
});
